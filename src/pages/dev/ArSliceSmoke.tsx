/**
 * DEV-only smoke entry that forces mock BLE and opens AR Slice controls.
 * Navigate to /dev/ar-slice-smoke
 */
import ArSliceLab from "@/pages/ArSliceLab";

export default function ArSliceSmoke() {
  return <ArSliceLab />;
}
